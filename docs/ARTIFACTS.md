# Release artefacts

## Committed here (`data/`)

| file | bytes | SHA-256 |
|---|---:|---|
| `LexiCore_5000.csv` | 303,824 | `49ae7a92ef5a9301286a6607f2f2e1e4b8a0f381784ad65996c36af187854de4` |
| `LexiCore_5000.json` | 960,335 | `f6b0b0567a9a5678feb9be675b4994162403e200d2b9feee7960524a7421bdf8` |
| `LexiCore_5000.txt` | 39,223 | `56d4fd064ff7e60c285382968a6e742d88ca2e95cea4b84ebecb687a89864faa` |
| `oxford_comparison_sets.csv` | 82,396 | `d14dffa352bd6859a4dbbef91684130f28392dd5d24bfc4ee552668beed821d0` |

Those four are byte-identical to the release and their hashes are also declared inside
`data/MANIFEST.json → artifacts` (`make checksums` verifies both).

`MANIFEST.json` itself is unmodified from the release (128,671 bytes); its own hash is in
`data/SHA256SUMS.txt`, which covers all 53 committed files under `data/`, `docs/`,
`ecosystem/` and `reports/`.

## Committed derived tables (`data/derived/`)

Regenerated by `make derived` (deterministic: same bytes on every machine, gzip `mtime=0`).
Row counts, byte sizes and SHA-256 for each file: `data/derived/derived_manifest.json`.

## In the release bucket, not committed (`out/*.pkl`)

Sizes and SHA-256 are recorded in [`checkpoint_artifacts.json`](checkpoint_artifacts.json),
generated by the bootstrap that built this repository. Summary:

| payload | bytes | content |
|---|---:|---|
| `src_fineweb.pkl` | 234,771,611 | per-source `meta`, `freq`, `df`, `part_freq`, `part_tot`, `year_freq`, `held` |
| `src_cc2026.pkl` | 193,147,559 | same shape |
| `src_wiki.pkl` | 133,066,397 | same shape |
| `src_subs.pkl` | 63,886,497 | same shape |
| `src_fwedu.pkl` | 28,601,461 | same shape |
| `features.pkl` | 63,306,804 | `table` (215,416 × 22 features) + `totals` (per-source tokens/docs/P/files/extra) |
| `labels.pkl` | 20,974,473 | `lemma → {level, source, confidence, model_proba}` |
| `selected.pkl` | 925,158 | the 5,000 chosen rows with full scoring detail |
| `surface_to_lemma.pkl` | 4,326,731 | 283,502 surface → lemma map |
| | **743,006,691** | |

GitHub rejects single files above 100 MB, so anything above that line can only live in a release
asset bucket / LFS. Add `src_*.pkl` to a GitHub Release (not the tree) if you need a public
download: the hashes above let any mirror be checked.

## Checkpoint metadata

`ckpt_lx5000v2_finalize_20260914T175041Z.tar.gz` · 374,861,085 bytes ·
SHA-256 `74aed1c2e249da6074dcc03ebbc00cfc4d1543efcbd8d080af0d8534acff26aa` · 44 entries ·
built 2026-09-14 12:59:33 → 17:50:41 UTC on a 24-core / 50.5 GB host (Python 3.13.15).

The checkpoint also carried `out/done/*.flag` (11 markers) — copies are kept under
`reports/upstream/done/` so the resume behaviour of the original runner stays inspectable.
